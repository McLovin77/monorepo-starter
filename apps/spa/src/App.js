import React from "react";
import { DSPrimaryButton } from "ds";

const App = () => {
	return (
		<div>
			<h1>SPA</h1>
			<DSPrimaryButton>Shared button</DSPrimaryButton>
		</div>
	);
};

export default App;
